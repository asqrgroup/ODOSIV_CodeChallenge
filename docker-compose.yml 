version: '3.8'

services:
  app:
    build: .
    container_name: my_app
    ports:
      - "80:80"   # Expose app on port 80 (HTTP)
    environment:
      DB_HOST: my-rds-instance.abcdefgh.us-east-1.rds.amazonaws.com
      DB_PORT: 5432
      DB_NAME: appdb
      DB_USER: appuser
      DB_PASSWORD: secret
